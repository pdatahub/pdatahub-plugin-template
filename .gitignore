node_modules/
dist/
coverage/
*.log
.env
.env.local
.DS_Store
*.tsbuildinfo

# Phantom lockfile from accidental npm install (we use pnpm)
package-lock.json

# Phantom lockfile from accidental npm install (we use pnpm)
package-lock.json
